"""Exact search over the delivery domain: A*, IDA*, alpha-beta, and the heuristics.

Every algorithm here is instrumented -- expansions, generated nodes, re-expansions,
effective branching factor, wall clock -- because the claims this repo makes are
comparative ("h2 expands fewer nodes than h1") and a comparative claim with no counter
behind it is a vibe.
"""
