# The C in src/ and host/ has NO dependencies at all: `make host` needs gcc and libm,
# nothing else. That is the point of the project -- the target board in BRIEF.md is an
# armv6 Raspberry Pi Zero, where the prebuilt numerical stack does not exist.
#
# The oracle and the analysis need numpy and nothing else. Every CSV in results/ is
# produced with numpy alone, and the test suite passes with numpy alone.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# The system interpreter on this machine is PEP-668 externally managed, so installing
# into it fails. Use the venv.

numpy>=1.26            # the only hard requirement for the Python side
matplotlib>=3.7        # analysis/plot_results.py only; nothing else imports it

# --- for the parts of BRIEF.md that need hardware (see STATUS.md) ---
# None of these are importable on this machine and none are used by anything here.
# They are recorded so the hardware path is a shopping list rather than a guess.
#
# smbus2>=0.4          # I2C for the ADXL345 accelerometer / INA219 current sensor
# adafruit-circuitpython-adxl34x
# adafruit-circuitpython-ina219
# sounddevice>=0.4     # I2S microphone capture via ALSA
# tflite-runtime>=2.14 # Zero 2 W autoencoder baseline. NO armv6 wheel exists, which is
#                      # the comparison BRIEF.md asks for and the reason its row is empty.
